# Milestone v1.2: Setup & CV Management

**Status:** ✅ SHIPPED 2026-04-27
**Phases:** 7–8
**Total Plans:** 3

## Overview

Eliminated the two biggest post-install friction points: (1) the app now auto-registers itself in the Linux system launcher on first packaged run, and (2) users can refresh `cv.md` from inside the Electron app via a native file picker — accepting `.md` files directly and `.pdf` files via text extraction with a review-and-confirm flow.

## Phases

### Phase 7: Desktop Shortcut Auto-Creation

**Goal**: Eliminate the launcher friction — after first run of the packaged app, JobEngine appears in the Linux system launcher exactly like a natively installed application
**Depends on**: Phase 1 (Electron main-process app lifecycle), Phase 2 (`write-file-atomic` write pattern for the `.desktop` file)
**Requirements**: DESK-01, DESK-02
**Plans**: 1

Plans:
- [x] 07-01-PLAN.md — Create services/desktop-shortcut.ts (ensureDesktopShortcut) and wire into electron/src/main/index.ts

**Success Criteria:**
1. After first launch of the packaged AppImage, `~/.local/share/applications/jobengine.desktop` exists and JobEngine appears in the user's system launcher / app menu
2. On every subsequent launch (packaged or dev), if the `.desktop` file already exists the app starts silently and never modifies it — manual edits by the user are preserved
3. Shortcut creation is silent: no UI prompt, no error popup, and no behavior change when running the unpackaged dev build

**Verification:** passed (4/5 truths verified; 1 UAT-deferred to packaged build)
**Completed:** 2026-04-27

---

### Phase 8: CV Upload & PDF Extraction

**Goal**: User keeps `cv.md` fresh from inside the Electron app — pick a file, see what will be saved, confirm, done — without ever opening a terminal or text editor
**Depends on**: Phase 2 (`lockAndWrite` + `write-file-atomic` pattern for `cv.md` overwrites), Phase 7
**Requirements**: CV-01, CV-02, CV-03, CV-04, CV-05
**Plans**: 2

Plans:
- [x] 08-01-PLAN.md — Backend + IPC contracts: install unpdf, pdf-extract service, openCvFilePicker / updateCv / getCvMtime IPC handles + preload bridges
- [x] 08-02-PLAN.md — UI: CvUploadModal (PDF review/edit) + CvConfirmModal (last-modified date + Replace) + CvPanel orchestration (Update CV button, upload state machine, post-write refresh)

**Success Criteria:**
1. User can open a file picker from inside the app and select either a `.md` or `.pdf` file as the new CV source
2. Selecting a `.md` file replaces `cv.md` byte-for-byte after the user confirms — no transformation
3. Selecting a `.pdf` file shows the user the extracted Markdown in an editable review pane before any write happens
4. Before either `.md` or `.pdf` content is written to `cv.md`, the user sees a confirmation prompt that displays the current `cv.md` last-modified date and gives the option to cancel
5. After confirming, `cv.md` is updated atomically and the in-app CV view reflects the new content without a manual reload

**Verification:** human_needed (12/12 code truths verified; 6 human UAT deferred to live Electron app)
**Completed:** 2026-04-27

---

## Milestone Summary

**Key Decisions:**
- Phase split (7 vs 8) chosen because the two features verify under different conditions: Phase 7 requires packaged build; Phase 8 verifies in dev mode
- `unpdf@1.6.0` chosen over `pdf-parse` / `pdfjs-dist` for tree-shaking, no native bindings, and pure JS extraction
- `seed-if-missing` guard before `lockAndWrite` required because `cv.md` can be absent; `lockAndWrite` always reads before write
- `getCvMtime()` fetched at confirm-modal-open time (not at file-pick time): ensures mtime is current if user deliberates on review modal
- `bg-ctp-mauve` color contract: reserved to the Replace button in CvConfirmModal only — set in UI-SPEC before planning

**Issues Deferred:**
- DESK-01 UAT (5 items): require packaged AppImage + $APPIMAGE/$APPDIR env — deferred to first packaged release
- Phase 8 human UAT (6 items): require live Electron app — deferred per project policy
- Dead import: `MAX_FILE_BYTES` imported in `ipc-handlers.ts` but unused (size guard fires inside `extractPdfText`)
- Stale log message: `console.log('[desktop-shortcut] created/updated')` — "updated" is stale wording

**Technical Debt Incurred:**
- DESK-01 REQUIREMENTS.md checkbox left unchecked — update to `[x]` after first packaged AppImage build verifies end-to-end
- 07-01-SUMMARY.md uses `provides` section instead of `requirements-completed` frontmatter — cosmetic inconsistency

**Audit:** `.planning/milestones/v1.2-MILESTONE-AUDIT.md` (status: tech_debt, 6/7 reqs satisfied at code level)

---

_For current project status, see .planning/ROADMAP.md_
