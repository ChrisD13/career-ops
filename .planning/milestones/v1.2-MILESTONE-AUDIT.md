---
milestone: v1.2
audited: 2026-04-27T22:00:00Z
status: tech_debt
scores:
  requirements: 6/7
  phases: 2/2
  integration: 7/7
  flows: 2/2
gaps: []
tech_debt:
  - phase: 07-desktop-shortcut-auto-creation
    items:
      - "DESK-01 UAT-deferred: end-to-end .desktop creation requires packaged AppImage run (UAT items 15-17)"
      - "DESK-01 UAT-deferred: icon copy from $APPDIR to ~/.local/share/icons/hicolor/256x256/apps/jobengine.png (UAT item 16)"
      - "DESK-01 UAT-deferred: JobEngine appears in system launcher after .desktop write (UAT item 17)"
      - "DESK-02 UAT-deferred: user-edited .desktop byte-for-byte unchanged after subsequent AppImage launch (UAT item 18)"
      - "DESK-01 recovery UAT-deferred: deleting .desktop causes recreation on next AppImage launch (UAT item 19)"
      - "Cosmetic: console.log says 'created/updated' but function can only ever create (not update) — stale wording from prior implementation"
  - phase: 08-cv-upload-pdf-extraction
    items:
      - "Human UAT deferred: native OS file dialog opens filtered to .md and .pdf (requires live app)"
      - "Human UAT deferred: .md flow goes directly to confirm modal (modal sequencing requires live Electron app)"
      - "Human UAT deferred: .pdf flow opens review modal with extracted text in monospace textarea (requires live app + real PDF)"
      - "Human UAT deferred: full Replace flow writes atomically and CvPanel refreshes (requires live app + filesystem)"
      - "Human UAT deferred: cancel at any step returns to idle with no toast/error (requires live app)"
      - "Human UAT deferred: >10 MB file shows InlineErrorBanner (requires live app + large test file)"
      - "Dead import: MAX_FILE_BYTES imported in ipc-handlers.ts but never used — size guard fires correctly inside extractPdfText() internally"
  - phase: requirements
    items:
      - "DESK-01 checkbox not updated in REQUIREMENTS.md — UAT-deferred per project policy (requires packaged AppImage)"
      - "07-01-SUMMARY.md uses 'provides' section instead of 'requirements-completed' frontmatter — DESK-01 and DESK-02 not listed in standard location"
nyquist:
  overall: skipped
  reason: "workflow.nyquist_validation=false"
---

# Milestone v1.2 Audit — Setup & CV Management

**Audited:** 2026-04-27
**Status:** tech_debt ⚡
**Phases:** 2/2 complete
**Score:** 6/7 requirements satisfied at code level (1 UAT-deferred)

All seven milestone requirements have confirmed integration wiring. No critical blockers. Accumulated UAT deferrals (11 items across 2 phases) require a packaged AppImage run before the project ships.

---

## Requirements Coverage (3-Source Cross-Reference)

| REQ-ID | Phase | VERIFICATION.md | SUMMARY frontmatter | REQUIREMENTS.md | Final Status |
|--------|-------|-----------------|---------------------|-----------------|--------------|
| DESK-01 | 7 | passed (UAT-deferred) | not listed | `[ ]` | **partial** — UAT deferred |
| DESK-02 | 7 | passed (✓ SATISFIED) | not listed | `[x]` ← updated | **satisfied** |
| CV-01 | 8 | human_needed (SATISFIED) | listed (08-01, 08-02) | `[x]` ← updated | **satisfied** |
| CV-02 | 8 | human_needed (SATISFIED) | listed (08-01) | `[x]` ← updated | **satisfied** |
| CV-03 | 8 | human_needed (SATISFIED) | listed (08-01) | `[x]` ← updated | **satisfied** |
| CV-04 | 8 | human_needed (SATISFIED code) | listed (08-02) | `[x]` ← updated | **satisfied** |
| CV-05 | 8 | human_needed (SATISFIED code) | listed (08-01, 08-02) | `[x]` ← updated | **satisfied** |

**FAIL gate:** No unsatisfied requirements — gaps_found not triggered.
**Orphan detection:** All 7 requirements have VERIFICATION.md coverage — no orphans.

### DESK-01 — Partial (UAT-Deferred)

DESK-01 is structurally satisfied in code (existsSync gate, writeFileAtomic call, correct `Exec=`/`Icon=`/`StartupWMClass=` fields in `buildDesktopEntry`). End-to-end verification requires a packaged AppImage with `$APPIMAGE` and `$APPDIR` environment variables set by the AppImage runtime. Deferred per project UAT policy (all human UAT deferred until feature-complete).

This is not a blocker — the code path is correct and has been verified at the static analysis level.

---

## Phase Verification Summary

| Phase | Status | Score | Gaps | Deferred |
|-------|--------|-------|------|----------|
| Phase 7: Desktop Shortcut Auto-Creation | passed | 4/5 | 0 critical | 5 UAT items (packaged build) |
| Phase 8: CV Upload & PDF Extraction | human_needed | 12/12 | 0 critical | 6 human UAT items |

---

## Cross-Phase Integration (7/7 checks passed)

Integration checker result: **all pass**. No broken flows, missing connections, or unprotected routes.

| Check | Status | Notes |
|-------|--------|-------|
| Phase 7→8 dependency: `lockAndWrite`/`write-file-atomic` in `updateCv` | PASS | `lockAndWrite(cvPath, () => content, pendingGuiWrites)` confirmed in ipc-handlers.ts:162–175 |
| `ensureDesktopShortcut()` wiring in index.ts | PASS | Line 104, after `setTimeout(initUpdater)`, fire-and-forget |
| CvPanel → IPC: `openCvFilePicker`, `updateCv`, `getCvMtime` | PASS | All three through `window.api.*` preload bridges |
| `CvUploadModal` + `CvConfirmModal` mounting in CvPanel | PASS | Conditional on `upload.kind === 'review'` / `'confirm'` |
| Post-write refresh: `await load()` after `updateCv` | PASS | State reset → re-read → ReactMarkdown re-renders |
| E2E .md flow: button → picker → confirm → write → refresh | PASS | Full trace verified |
| E2E .pdf flow: button → picker → review modal → confirm → write → refresh | PASS | Full trace verified |

### Requirements Integration Map

| REQ-ID | Integration Path | Status |
|--------|-----------------|--------|
| DESK-01 | `index.ts:104` → `ensureDesktopShortcut()` → `writeFileAtomic(target, newEntry)` | WIRED |
| DESK-02 | `desktop-shortcut.ts:44` `existsSync(target)` unconditional early-return | WIRED |
| CV-01 | CvPanel button → `window.api.openCvFilePicker` → `dialog.showOpenDialog` | WIRED |
| CV-02 | `.md` branch → `{ type: 'md', content }` → skip review → `lockAndWrite` | WIRED |
| CV-03 | `.pdf` branch → `extractPdfText(filePath)` via unpdf → review/confirm → `lockAndWrite` | WIRED |
| CV-04 | `upload.kind === 'review'` mounts `CvUploadModal` with editable textarea | WIRED |
| CV-05 | `getCvMtime()` at confirm-open time → `CvConfirmModal` displays `formatMtime(mtimeIso)` | WIRED |

---

## Tech Debt

### Phase 07 — Desktop Shortcut Auto-Creation

| # | Item | Severity | Action |
|---|------|----------|--------|
| 1 | DESK-01 end-to-end (UAT items 15–19): packaged AppImage run required for `.desktop` creation, icon copy, launcher registration, idempotency, and recovery | Deferred | UAT backlog — verify on first packaged build |
| 2 | Cosmetic: `console.log` says 'created/updated' but function can only ever create | Info | Rename log message to 'created' |

### Phase 08 — CV Upload & PDF Extraction

| # | Item | Severity | Action |
|---|------|----------|--------|
| 1–6 | 6 human UAT items (file dialog, .md flow, .pdf flow, full replace, cancel flow, oversized file error) | Deferred | UAT backlog — verify with live Electron app |
| 7 | `MAX_FILE_BYTES` imported in ipc-handlers.ts but never referenced — dead import (size guard fires correctly inside `extractPdfText` internally) | Info | Remove dead import |

### REQUIREMENTS.md

| # | Item | Severity | Action |
|---|------|----------|--------|
| 1 | DESK-01 checkbox remains `[ ]` — UAT-deferred; update to `[x]` after packaged AppImage verification | Deferred | Close after UAT |
| 2 | 07-01-SUMMARY.md uses `provides` section rather than `requirements-completed` frontmatter for DESK-01/DESK-02 | Info | No action required — provides section is equivalent for this phase |

### Total: 13 deferred/info items across 2 phases

---

## Nyquist Compliance

Skipped — `workflow.nyquist_validation=false`.

---

## Anti-Patterns

| Phase | File | Finding | Severity |
|-------|------|---------|----------|
| 07 | `desktop-shortcut.ts:63` | `console.log` says 'created/updated' — "updated" is stale wording | Info |
| 08 | `ipc-handlers.ts:23` | `MAX_FILE_BYTES` imported but unused — dead import | Info |
| 07 | `07-01-SUMMARY.md` | No `requirements-completed` frontmatter field — uses `provides` section instead | Info |

---

## Conclusion

Milestone v1.2 has no critical blockers. All 7 requirements have correct integration wiring and are satisfied at the code level. The accumulated tech debt consists entirely of:
1. UAT items deferred per project policy (packaged AppImage + live Electron app required)
2. Minor cosmetic/code-hygiene items (dead import, stale log message)

**Recommended next step:** Complete milestone → start v1.3, schedule UAT backlog verification on first packaged build.
